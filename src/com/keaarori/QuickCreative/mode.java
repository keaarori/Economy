package com.keaarori.QuickCreative;
import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;

public class mode {
        public static void mode (CommandSender sender, Command command,String label,String[] args)
    {
        // needs to display you need to deposit error.
    sender.sendMessage("This is your mode!");
    }
}
